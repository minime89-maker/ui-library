import React from 'react'
import DropdownFilled from './DropdownFilled'

export default {
	title: 'Example/Dropdown',
	component: DropdownFilled,
}

const Template = (args) => <DropdownFilled {...args} />
export const Filled = Template.bind({})
